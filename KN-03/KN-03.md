# Docker-347

# KN03: Netzwerk, Sicherheit

**Modul 347 – Dienst mit Container anwenden**
**Autoren:** David Tarlos, Leon Wulff

## Inhaltsverzeichnis

- [A) Eigenes Netzwerk](#a-eigenes-netzwerk)
  - [1. Aufbau des Zielsystems](#1-aufbau-des-zielsystems)
    - [tbz-Netzwerk anlegen](#tbz-netzwerk-anlegen)
    - [Vier busybox-Container starten](#vier-busybox-container-starten)
    - [Container laufen](#container-laufen)
  - [2. IP-Adressen der Container](#2-ip-adressen-der-container)
  - [3. Interaktive Session in busybox1 (default bridge)](#3-interaktive-session-in-busybox1-default-bridge)
  - [4. Interaktive Session in busybox3 (tbz-Netz)](#4-interaktive-session-in-busybox3-tbz-netz)
  - [5. Gemeinsamkeiten, Unterschiede und Schlussfolgerung](#5-gemeinsamkeiten-unterschiede-und-schlussfolgerung)
- [B) Reflexion und Verbesserung von KN02](#b-reflexion-und-verbesserung-von-kn02)
  - [In welchem Netzwerk befanden sich Web- und DB-Container?](#in-welchem-netzwerk-befanden-sich-web--und-db-container)
  - [Weshalb funktionierte die Verbindung über die IP / den Container-Namen?](#weshalb-funktionierte-die-verbindung-über-die-ip--den-container-namen)
  - [Weshalb ist eine direkt eingetragene Container-IP nicht ideal?](#weshalb-ist-eine-direkt-eingetragene-container-ip-nicht-ideal)
  - [Verbesserungsvorschlag](#verbesserungsvorschlag)
  - [Umsetzung der Verbesserung](#umsetzung-der-verbesserung)
  - [Test der Verbesserung](#test-der-verbesserung)

---

## A) Eigenes Netzwerk

### 1. Aufbau des Zielsystems

Das Zielsystem besteht aus vier `busybox`-Containern in zwei Netzwerken:

```
default bridge (172.17.0.0/16)      tbz (172.18.0.0/16)
├── busybox1                        ├── busybox3
└── busybox2                        └── busybox4
```

#### tbz-Netzwerk anlegen

Das user-defined Netzwerk `tbz` wird mit dem vorgegebenen Subnet `172.18.0.0/16` erstellt. Das default bridge Netzwerk existiert bereits und ist nicht veränderbar.

```bash
docker network create --subnet 172.18.0.0/16 tbz
docker network ls
```

![docker network create und docker network ls](screenshots/01_network_create.png)

Vor dem Befehl existieren nur die Standard-Netzwerke (`bridge`, `host`, `none`) sowie ein älteres Compose-Netz (`minecraft-server_default`). Nach dem Befehl ist `tbz` in der Liste sichtbar – mit Driver `bridge` und Scope `local`.

#### Vier busybox-Container starten

busybox1 und busybox2 landen ohne `--network`-Flag automatisch im default bridge. busybox3 und busybox4 werden explizit dem `tbz`-Netz zugewiesen. Das Argument `sleep infinity` hält die Container am Leben, damit später interaktive Sessions möglich sind.

```bash
docker run -dit --name busybox1 busybox sleep infinity
docker run -dit --name busybox2 busybox sleep infinity
docker run -dit --name busybox3 --network tbz busybox sleep infinity
docker run -dit --name busybox4 --network tbz busybox sleep infinity
```

![Vier docker run-Befehle für die busybox-Container](screenshots/02_container_run.png)

#### Container laufen

```bash
docker ps
```

![docker ps zeigt alle vier busybox-Container](screenshots/03_docker_ps.png)

Alle vier Container haben den Status `Up`.

---

### 2. IP-Adressen der Container

Die IP-Adressen lassen sich am übersichtlichsten über `docker network inspect` ablesen:

```bash
docker network inspect bridge
docker network inspect tbz
```

**Default bridge:**

![docker network inspect bridge](screenshots/04_inspect_bridge.png)

**tbz-Netzwerk:**

![docker network inspect tbz](screenshots/05_inspect_tbz.png)

**Zusammenfassung:**

| Container | Netzwerk      | IP-Adresse     | Subnet           | Gateway       |
|-----------|---------------|----------------|------------------|---------------|
| busybox1  | default bridge | `172.17.0.2`  | `172.17.0.0/16` | `172.17.0.1` |
| busybox2  | default bridge | `172.17.0.3`  | `172.17.0.0/16` | `172.17.0.1` |
| busybox3  | tbz            | `172.18.0.2`  | `172.18.0.0/16` | `172.18.0.1` |
| busybox4  | tbz            | `172.18.0.3`  | `172.18.0.0/16` | `172.18.0.1` |

Die IP-Adressen wurden von Docker automatisch vergeben (DHCP-ähnlich). Die erste IP des Subnets ist jeweils das Gateway = der Host.

---

### 3. Interaktive Session in busybox1 (default bridge)

```bash
docker exec -it busybox1 sh
```

In der Container-Shell:

```sh
ifconfig
route -n
ping -c 3 busybox2
ping -c 3 busybox3
ping -c 3 172.17.0.3
ping -c 3 172.18.0.2
```

![busybox1 Session: ifconfig + Pings](screenshots/06_busybox1_session.png)

![busybox1 route -n zeigt den Default-Gateway](screenshots/07_busybox1_route.png)

**Beobachtungen:**

| Test                         | Ergebnis           | Erklärung |
|------------------------------|--------------------|-----------|
| `ifconfig`                   | `eth0: 172.17.0.2` | IP von busybox1 |
| `route -n` Default-Gateway   | `172.17.0.1`       | Host-Interface im default bridge. **Gleicher Gateway wie busybox2** (beide im selben Netzwerk). |
| `ping busybox2`              | `bad address 'busybox2'` | Default bridge hat **keine DNS-Auflösung** – Container-Namen können nicht zu IPs aufgelöst werden. |
| `ping busybox3`              | `bad address 'busybox3'` | Gleicher Grund: keine Namensauflösung. |
| `ping 172.17.0.3` (busybox2) | `0% packet loss` ✅ | Gleiches Netzwerk → direkte Erreichbarkeit über IP. |
| `ping 172.18.0.2` (busybox3) | `100% packet loss` ❌ | Anderes Netzwerk → durch Docker isoliert, keine Pakete kommen durch. |

---

### 4. Interaktive Session in busybox3 (tbz-Netz)

```bash
docker exec -it busybox3 sh
```

In der Container-Shell:

```sh
ifconfig
route -n
ping -c 3 busybox1
ping -c 3 busybox4
ping -c 3 172.17.0.2
ping -c 3 172.18.0.3
```

![busybox3 Session: ifconfig + route -n + Pings](screenshots/08_busybox3_session.png)

**Beobachtungen:**

| Test                         | Ergebnis           | Erklärung |
|------------------------------|--------------------|-----------|
| `ifconfig`                   | `eth0: 172.18.0.2` | IP von busybox3 |
| `route -n` Default-Gateway   | `172.18.0.1`       | Host-Interface im tbz-Netz. **Gleicher Gateway wie busybox4** (beide im selben Netzwerk). |
| `ping busybox1`              | `bad address 'busybox1'` | busybox1 ist in einem anderen Netz – im tbz-Netz nicht bekannt. |
| `ping busybox4`              | `0% packet loss` ✅ | tbz ist ein **user-defined Netzwerk** und hat eingebaute DNS-Auflösung. Der Name wird zur IP `172.18.0.3` aufgelöst und der Ping kommt durch. |
| `ping 172.17.0.2` (busybox1) | `100% packet loss` ❌ | Anderes Netzwerk → isoliert. |
| `ping 172.18.0.3` (busybox4) | `0% packet loss` ✅ | Gleiches Netz → direkter Weg. |

---

### 5. Gemeinsamkeiten, Unterschiede und Schlussfolgerung

**Gemeinsamkeiten der beiden Netzwerke:**

- Beide sind **Bridge-Netzwerke**. Jeder Container bekommt automatisch eine IP aus dem jeweiligen Subnet zugewiesen.
- In **beiden** Netzen können Container, die im **selben** Netz liegen, sich gegenseitig per IP-Adresse erreichen (ping per IP funktioniert immer im eigenen Netz).
- Der Default-Gateway jedes Containers ist der **Host** – jeweils mit der ersten IP des Subnets (`172.17.0.1` bzw. `172.18.0.1`). Der Host ist also gleichzeitig in beiden Netzwerken Mitglied.

**Unterschiede:**

| Eigenschaft           | default bridge                        | user-defined bridge (tbz)            |
|-----------------------|----------------------------------------|--------------------------------------|
| DNS-Auflösung von Container-Namen | **nein** – nur per IP möglich | **ja** – `ping busybox4` funktioniert |
| Subnet/IP-Range       | von Docker vorgegeben (`172.17.0.0/16`) | beim Erstellen selbst wählbar        |
| Konfigurierbar zur Laufzeit | nur eingeschränkt                | flexibler                            |
| Isolation             | Container darin sind unter sich       | Container darin sind unter sich, **getrennt von anderen Netzen** |

**Wie kommen die Zustände zustande?**

Docker verwaltet pro Bridge-Netzwerk eine eigene virtuelle Linux-Bridge (`docker0` für default, `br-<id>` für tbz). Jeder Container bekommt ein virtuelles Netzwerk-Interface, das mit der entsprechenden Bridge verbunden ist. Pakete innerhalb derselben Bridge werden direkt weitergeleitet, **zwischen verschiedenen Bridges blockiert Docker per iptables-Regel** den Verkehr standardmässig. Daher das `100% packet loss` bei Pings über die Netzgrenze hinweg.

Die DNS-Auflösung in user-defined Netzwerken liefert ein eingebauter Docker-DNS-Server (`127.0.0.11`), der die Container-Namen kennt. Im default bridge ist dieser Mechanismus nicht aktiv.

**Schlussfolgerung:**

Wer Container braucht, die sicher und komfortabel miteinander kommunizieren sollen, **verwendet ein user-defined Netzwerk**. Vorteile:

1. **Container-Namen statt IPs:** Stabil, auch wenn IPs sich beim Neustart ändern.
2. **Isolation gegen aussen:** Andere Container ausserhalb des Netzes haben keinen Zugriff.
3. **Klare Architektur:** Jede Anwendung kann ihr eigenes Netz haben (z.B. eines pro Projekt).

Die Docker-Dokumentation empfiehlt user-defined Netzwerke deshalb ausdrücklich – das default bridge gilt als Legacy.

---

## B) Reflexion und Verbesserung von KN02

In KN02 wurde ein Web-Container (PHP/Apache) und ein DB-Container (MariaDB) aufgebaut. Die Web-App spricht die DB an, um eine Verbindung zu prüfen.

### In welchem Netzwerk befanden sich Web- und DB-Container?

Beide Container liefen im **default bridge** Netzwerk. Der Web-Container wurde zusätzlich über das damals gängige (aber deprecated) `--link kn02b-db`-Flag mit dem DB-Container verknüpft:

```bash
docker run -d --name kn02b-web -p 80:80 --link kn02b-db leonwul/m347:kn02b-web
```

### Weshalb funktionierte die Verbindung über die IP / den Container-Namen?

Die Aufgabenstellung fragt explizit nach einer Verbindung über die **IP-Adresse**. In unserer KN02-Umsetzung wurde **nicht** direkt eine fixe IP eingetragen, sondern stattdessen der Container-Name `kn02b-db` mit Hilfe des `--link`-Flags verwendet. Der Mechanismus dahinter ist aber funktional vergleichbar mit einer fixen IP – deshalb behandeln wir beide Fälle gemeinsam:

- **Variante "fixe IP eintragen":** Wenn man im PHP-Code direkt `$host = '172.17.0.3'` setzt, funktioniert die Verbindung, weil beide Container im selben default bridge sind und somit per IP erreichbar (vgl. das Pingen per IP im Teil A). Innerhalb des gleichen Bridge-Netzes routet Docker den Verkehr direkt.
- **Variante `--link` (unser Fall):** `--link` schreibt beim Container-Start einen Eintrag in `/etc/hosts` des Web-Containers, der den Namen `kn02b-db` auf die aktuelle IP des DB-Containers auflöst. Das funktioniert nur, weil beide im selben Netzwerk sind und Docker beim Linken die IP zum Zeitpunkt des Starts in die Hosts-Datei einträgt.

Beide Varianten setzen voraus, dass die Container im gleichen Netzwerk liegen.

### Weshalb ist eine direkt eingetragene Container-IP nicht ideal?

Mehrere Gründe sprechen klar gegen das Hardcoden einer IP (und in abgeschwächter Form auch gegen `--link`):

1. **IPs ändern sich:** Beim Stoppen/Neustarten eines Containers kann Docker eine andere IP aus dem Pool vergeben. Die App findet die DB dann nicht mehr und ist kaputt.
2. **Nicht portabel:** Bei einem Deployment auf einem anderen Rechner haben die Container fast garantiert andere IPs als auf dem Entwicklungssystem.
3. **Schwer wartbar:** Die IP ist im Code/Config versteckt. Beim Ändern müsste man alle Stellen im Code finden und anpassen.
4. **`--link` ist deprecated:** Docker selbst empfiehlt, `--link` nicht mehr zu verwenden, sondern auf user-defined Netzwerke umzusteigen.
5. **Sicherheits-Aspekt:** Die Container liegen im default bridge, in dem alle anderen "namenlosen" Container ebenfalls landen. Es gibt keine saubere Trennung von Anwendungen voneinander. Zusätzlich war der DB-Port (`-p 3306:3306`) nach aussen exponiert, obwohl die DB eigentlich nur vom Web-Container intern erreichbar sein muss – ein Verstoss gegen das **Principle of Least Privileges**.

### Verbesserungsvorschlag

Statt im default bridge zu arbeiten und `--link` zu verwenden, legen wir ein **eigenes user-defined Netzwerk** (`kn02-net`) an und packen beide Container hinein. Damit gewinnen wir:

- **DNS-Auflösung über Container-Namen** – die `db.php` kann weiterhin `$host = 'kn02b-db'` verwenden, ohne dass `--link` nötig ist. Docker löst den Namen automatisch zur aktuellen IP auf.
- **Isolation:** Andere Container ausserhalb von `kn02-net` haben keinen Zugriff auf die DB.
- **Port 3306 wird nicht mehr nach aussen gemappt:** Die DB ist nur noch innerhalb von `kn02-net` erreichbar (Principle of Least Privileges).
- **Bessere Wartbarkeit:** Wenn DB oder Web neu starten und neue IPs bekommen, funktioniert die Anwendung weiterhin.

### Umsetzung der Verbesserung

**1. Eigenes Netzwerk erstellen:**

```bash
docker network create kn02-net
```

**2. Images neu builden:**

Die Dockerfiles aus KN02 bleiben unverändert. Auch die `db.php` muss nicht angepasst werden – `$host = 'kn02b-db'` funktioniert weiterhin, da der Container-Name jetzt über den Docker-DNS aufgelöst wird (statt über den `--link`-Trick mit `/etc/hosts`).

```bash
docker build -f Dockerfile.kn02b-db  -t kn02b-db  .
docker build -f Dockerfile.kn02b-web -t kn02b-web .
```

**3. Container starten – beide im selben user-defined Netz, DB ohne Port-Mapping nach aussen:**

```bash
docker run -d --name kn02b-db  --network kn02-net               kn02b-db
docker run -d --name kn02b-web --network kn02-net -p 80:80      kn02b-web
```

Wichtigste Änderungen gegenüber KN02:

- Beide Container haben `--network kn02-net` statt `--link`.
- Der DB-Container hat **kein** `-p 3306:3306` mehr → DB ist nur intern erreichbar.

![Build, Run und docker ps der verbesserten Variante](screenshots/09_kn02_build_run.png)

Im `docker ps`-Output ist die Sicherheits-Verbesserung deutlich sichtbar:

```
CONTAINER ID   IMAGE       ...   PORTS                NAMES
b402674103ae   kn02b-web   ...   0.0.0.0:80->80/tcp   kn02b-web
99a7dab7d8b2   kn02b-db    ...   3306/tcp             kn02b-db
```

Der Web-Container hat ein Port-Mapping (`0.0.0.0:80->80/tcp`) – ist also vom Host und damit vom Browser erreichbar. Der DB-Container zeigt nur `3306/tcp` ohne `0.0.0.0:`-Prefix – der Port ist **nur innerhalb** des Container-Netzes offen, **nicht** vom Host.

### Test der Verbesserung

Aufruf von `http://localhost/db.php` im Browser:

![db.php zeigt "Verbindung zur Datenbank erfolgreich auf Host kn02b-db"](screenshots/10_browser_db_success.png)

Die Ausgabe lautet:

> **Verbindung zur Datenbank erfolgreich auf Host kn02b-db**

Die Verbesserung ist damit nachgewiesen:

- Die Webseite erreicht die Datenbank weiterhin.
- Die Verbindung läuft jetzt sauber über den **Container-Namen** via Docker-DNS – ohne `--link` und ohne fixe IP.
- Die DB ist von aussen nicht mehr direkt zugänglich (nur über den Web-Container im gleichen Netz).
