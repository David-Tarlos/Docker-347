apiVersion: v1
kind: Secret
metadata:
  name: m347-secret
type: Opaque
stringData:
  # Plain values for readability — Kubernetes encodes them to base64 internally
  # when 'stringData' is used.
  DB_PASSWORD: "M347KN08PW"
  # Full .NET ConnectionString consumed by the Account container via env var.
  # appsettings.json reads this name from the environment when present.
  ConnectionStrings__Default: "Server=m347-kn08-db.cjknyrkikwm0.us-east-1.rds.amazonaws.com;Database=m347kn08;User ID=admin;Password=M347KN08PW;"
