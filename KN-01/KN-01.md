# Docker-347 – KN-01

## A) Installation (20%)

### Erster Container erstellt – Webseite

Screenshot der Webseite, nachdem der erste Container erstellt wurde:

<img width="1919" height="978" alt="Webseite des ersten Containers" src="https://github.com/user-attachments/assets/46019b36-53b1-45bc-9141-9cfe6050e139" />

### Container in Docker Desktop

Screenshot des laufenden Containers in Docker Desktop:

<img width="1574" height="662" alt="Container in Docker Desktop" src="https://github.com/user-attachments/assets/3daffdfe-15b1-43e9-8fb0-cad56e08268d" />

---

## B) Docker Command Line Interface (CLI) (50%)

Alle ausgeführten Befehle inklusive Erklärungen sind in der Datei
[`befehle.md`](./befehle.md) im selben Verzeichnis dokumentiert.

### Screenshot der CLI

<img width="1919" height="551" alt="Docker CLI Befehle" src="https://github.com/user-attachments/assets/442f90b3-f5a3-44d3-9b8d-81acc7498929" />

### Erklärungen zu den Container-Befehlen

#### Teil 1 – `docker run -d ubuntu`

> Erstellen und starten Sie einen Container mit dem Befehl `docker run -d`.
> Kommentieren Sie in 3–5 Sätzen, was mit dem Container geschieht.
> Wurde das Image automatisch heruntergeladen? Konnte es starten?

**Antwort:** Das Ubuntu-Image wurde automatisch von Docker Hub
heruntergeladen, da es lokal noch nicht vorhanden war. Der Container
wurde erstellt und gestartet, hat sich aber sofort wieder beendet
(Status `Exited`). Grund dafür ist, dass das Ubuntu-Image keinen
langlaufenden Hauptprozess besitzt – anders als z. B. nginx, das einen
Webserver-Prozess mitbringt. Ein Container lebt nur so lange wie sein
Hauptprozess; ohne aktiven Prozess endet er sofort. Das Flag `-d`
(detached) macht hier keinen Unterschied, weil es gar nichts gibt, was
im Hintergrund laufen könnte.

#### Teil 2 – `docker run -it ubuntu`

> Erstellen und starten Sie einen Container mit dem Befehl `docker run -it`.
> Kommentieren Sie in 3–5 Sätzen, was nun geschieht.

**Antwort:** Mit den Flags `-i` (interactive) und `-t` (TTY) wird beim
Start des Containers eine interaktive Shell geöffnet. Man landet direkt
im Bash-Prompt des Ubuntu-Containers (z. B. `root@abc123:/#`) und kann
Linux-Befehle ausführen, als wäre man auf einem normalen Ubuntu-System.
Die Bash ist nun der Hauptprozess des Containers und hält ihn am Leben,
solange die Shell offen bleibt. Mit dem Befehl `exit` wird die Shell
beendet – und damit auch der Container, weil der Hauptprozess endet.
Das zeigt, dass Ubuntu nur dann sinnvoll als Container nutzbar ist, wenn
man ihm aktiv einen Prozess (wie eine Shell) zur Verfügung stellt.

### `service nginx status` im Container

> Führen Sie den Befehl `service nginx status` aus. Erstellen Sie einen
> Screenshot des Befehls und des Resultats.

<img width="580" height="76" alt="service nginx status im Container" src="https://github.com/user-attachments/assets/150acb1f-a743-4910-a4fb-8b6dee534b33" />

### Status aller Container (`docker ps -a`)

> Überprüfen Sie den Status der Container. Erstellen Sie einen Screenshot
> des Befehls und des Resultats.

<img width="1919" height="721" alt="docker ps -a Resultat" src="https://github.com/user-attachments/assets/255bec7f-0f41-4f29-bda4-0dbc7f90c3f3" />

---

## Abgaben

- [x] Screenshot 1 – Webseite des ersten Containers (Teil A)
- [x] Screenshot 2 – Container in Docker Desktop (Teil A)
- [x] [`befehle.md`](./befehle.md) – alle CLI-Befehle mit Erklärungen
- [x] Screenshot 3 – Docker CLI Befehle (Teil B)
- [x] Screenshot 4 – `service nginx status`
- [x] Screenshot 5 – `docker ps -a`
